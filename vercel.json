{
  "buildCommand": "npm run build -w packages/shared-types && npm run build -w apps/frontend",
  "installCommand": "npm ci",
  "outputDirectory": "apps/frontend/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
